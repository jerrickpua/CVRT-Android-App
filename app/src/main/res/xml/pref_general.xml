<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!--<SwitchPreference-->
        <!--android:key="example_switch"-->
        <!--android:title="@string/pref_title_social_recommendations"-->
        <!--android:summary="@string/pref_description_social_recommendations"-->
        <!--android:defaultValue="true" />-->

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:key="recording_interval"
        android:title="@string/pref_title_display_name"
        android:defaultValue="1"
        android:selectAllOnFocus="true"
        android:inputType="number"
        />
    <EditTextPreference
        android:key="server_url"
        android:title="@string/pref_title_server_url_display_name"
        android:selectAllOnFocus="true"
        android:inputType="textUri"
        />
    <EditTextPreference
        android:key="location_service_update_interval"
        android:title="@string/pref_title_location_service_update_interval_display_name"
        android:selectAllOnFocus="true"
        android:defaultValue="5000"
        android:inputType="number"
        />
    <EditTextPreference
        android:key="location_service_fastest_update_interval"
        android:title="@string/pref_title_location_service_fastest_update_interval_display_name"
        android:defaultValue="2500"
        android:selectAllOnFocus="true"
        android:inputType="number"
        />
    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <!--<ListPreference-->
        <!--android:key="example_list"-->
        <!--android:title="@string/pref_title_add_friends_to_messages"-->
        <!--android:defaultValue="-1"-->
        <!--android:entries="@array/pref_example_list_titles"-->
        <!--android:entryValues="@array/pref_example_list_values"-->
        <!--android:negativeButtonText="@null"-->
        <!--android:positiveButtonText="@null" />-->

</PreferenceScreen>
